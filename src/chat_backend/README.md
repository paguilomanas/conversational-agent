# Backend – RAG API Service
#
# This is the FastAPI backend that powers the RAG pipeline.
# It exposes REST endpoints for document upload, processing,
# and conversational querying backed by retrieval-augmented generation.
#
# Run locally:  uvicorn app.main:app --reload
