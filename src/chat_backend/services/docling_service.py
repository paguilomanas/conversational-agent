"""
docling_service.py – Docling integration.

Sends documents to the Docling service via HTTP for parsing and
text extraction. Returns the structured text output that can then
be chunked and embedded.
"""
