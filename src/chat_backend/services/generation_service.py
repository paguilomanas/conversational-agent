"""
generation_service.py – LLM response generation.

Takes retrieved context chunks and the user question, constructs
a prompt using the configured template, calls the LLM API, and
returns the generated answer.
"""
