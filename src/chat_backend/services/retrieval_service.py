"""
retrieval_service.py – Vector retrieval.

Queries the Chroma vector database with an embedded question
and returns the top-k most relevant document chunks along with
their metadata (source file, page number, score).
"""
