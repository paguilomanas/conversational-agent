"""
storage_service.py – MinIO object storage interaction.

Provides helper functions to upload, download, list, and delete
objects in the configured MinIO bucket. Used to persist raw
uploaded documents and any processed artefacts.
"""
