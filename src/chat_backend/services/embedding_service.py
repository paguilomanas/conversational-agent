"""
embedding_service.py – Embedding generation.

Wraps the chosen embedding model (e.g. OpenAI, Sentence-Transformers)
and provides a unified interface to convert text chunks into vector
representations used for similarity search.
"""
